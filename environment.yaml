name: coil-sketching-4d
channels:
  - nvidia/label/cuda-12.4.1 # Note: modifications may be needed if on a different version of cuda
  - conda-forge

dependencies:
  - python>=3.9
  - matplotlib
  - numpy
  - cuda-toolkit
  - cupy
  - cudnn
  - nccl
  - pip
  - pip:
    - nibabel
    - matplotlib
    - jupyter    
    - pynvml
    - torch
    - antspyx
    - nibabel
